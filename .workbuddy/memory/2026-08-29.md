# 2026-08-29

- 测试 GitHub token：环境变量 GITHUB_TOKEN（账号 liusihua123）验证 HTTP 200，对 ayflying/study_planet 有 push 权限；用固定 DNS 解析（http.curloptResolve github.com→20.205.243.166）+ 环境变量 token 成功推送临时分支 token-push-test 并删除，更新/推送能力确认可用。
- 网络坑：环境变量代理 127.0.0.1:61399 已失效（无进程监听），git 直连需固定解析；已记入用户级 MEMORY.md。
- 用户约定：**每次修改完内容 → VERSION 文件 +1 → git 提交并推送**（持久规则，每轮交付都必须执行）。

## 2026-08-30 凌晨（合并远程+推送攻坚）
- 合并远程 ayflying 侧提交：0606165 移除 SQLite 全用 MySQL、家长隔离/Casdoor SSO、CI deploy-pro(f3275a7/fb79803)、0.3.2 版本号(d8e8f26)、诊断 workflow+代码规范(→4fc5edb)。本地未提交的 SQLite 兼容改动（contentlib/leaderboard/seed 的 ON CONFLICT 分支）因远程已删除 SQLite 而丢弃；仅保留 battle.go 死锁修复（onAnswer 重入 mutex + send/leave 关闭防竞争）。
- 版本号推进：远程占用 0.3.2 → 本地顺延 0.3.3（8fe96b0），合并提交 63b011a、4cd4878。
- 最终推送成功：origin/main = 4cd4878，本地与远程同步。go build 通过。
- **重大教训：Git Bash (mingw64 git 2.55.0.3) 在本机网络下 push/fetch/ls-remote 会"假成功"——exit 0 且无任何输出但实际未连接 GitHub**。可靠通道是 PowerShell 里的 git（2.55.0.4），能输出真实错误/结果。今后 git 网络操作必须用 PowerShell 验证，bash 的 exit 0 不可信。
- 推送命令模板（PowerShell）：`git -c credential.helper= -c credential.helper=store -c http.curloptResolve="github.com:443:<活IP>" push origin main`，env 代理需清空（$env:http_proxy=""）。
- 远程节奏快：等推送窗口期间对方连续提交（SQLite 移除→家长隔离→CI→0.3.2→诊断），每次推送前必须重新 fetch 合并，不可盲目 push。
- 本轮完成：填空题生成器(gen_fill.go)、宠物模块(000007_pet迁移+pet.go+pet api)、SQLite 本地部署支持(env 配置 driver=sqlite，恢复 mysql/sqlite 双迁移+db.go/gdbinit.go 双分支)、judge 共享判分包(填空数值判分)、battle_api.go(段位榜/历史)；go build ./... 通过，待接 WebSocket 对战引擎与前端。
- v0.2.2 提交推送：amend 作者为 liusihua <1129985028@qq.com>（repo-local git identity 已配置），force-with-lease 成功。
- v0.2.4 修复对战引擎：qIndex 从未递增导致卡第 1 题（nextQuestion 前移+room 起始 -1）、机器人播报 tick 带 Remain=0 会清空真人倒计时（改独立 opp_done 消息）；已推送 b55cef7。
- v0.2.3 完成：internal/battle 对战引擎（WS /ws/battle 匹配→5题×10秒速度计分→机器人兜底→段位落库+结算奖励文案）、REST 接线（battle rank/history + pet 五接口）、前端（对战 HUD+倒计时环+结算画面+段位主页+宠物页+填空题输入框+全局键盘 数字键/回车）；go build + vite build 全过；已推送 origin/main (3145ab1)。
- 工具链：本机无 Go，便携版在 C:\Users\liusihua\.workbuddy\binaries\golang\go（1.25.0）；client 首次 npm install 后 vite build 可用。
