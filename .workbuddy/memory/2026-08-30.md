# 2026-08-30 工作日志

## 上午：代码组织规范写入文档 + v0.3.4 发布
- 用户要求：代码不要全写一个文件、按模块拆分、可复用方法抽象出来，并写入文档。
- 修改 `docs/development.md`「代码组织规范（硬性要求）」一节：新增"禁止把代码全写到一个文件，新功能先按模块划分落点再动手"总则 + 提交前自查清单（4 项）。该节原有内容（200 行上限、抽象位置约定）为远程合并时已带入。
- 规则同步写入项目记忆 MEMORY.md（代码组织规范一节）。
- VERSION 0.3.3 → 0.3.4，提交 cb5e658，推送成功（远程确认 cb5e658）。

## 补充：超长文件盘点 + gitignore 补丁（v0.3.5）
- 统计超 200 行文件：battle.go 829、handlers.go 813、practice.go 343、pet.go 337、battle_api.go 271、gen_math.go 268、content.go 221、App.vue 301——后续改造时按规范逐步拆分。
- .gitignore 补充 `*.exe` / `*.exe~`（本地编译产物 studyplanet.exe 不再出现在未跟踪列表），v0.3.5 提交 c092cbc，推送成功并经 ls-remote 核实远程 = c092cbc。

## 下午：执行代码拆分（v0.3.6，提交 c529e18，已推送核实）
- **battle.go（829 行）→ 5 文件**：battle.go（引擎入口/writePump/send，113 行）、protocol.go（结构/常量/消息，135）、match.go（匹配/开房/抽题，164）、round.go（推题/计时/判分/机器人，219）、settle.go（结算/落库/奖励文案，220）、tier.go（段位规则唯一实现，公开 TierName/TrophiesDelta）。
- **handlers.go（813 行）→ 8 文件**：handlers.go 仅留 Health+废弃占位（40 行）、helpers.go（Record 转换/resolveChild/归属校验/ensureParentAuth/ensureName 公共助手）、words.go、reading.go、math.go、tasks.go、points.go（积分+奖励+兑换）、students.go（学生账号管理）。
- **消除重复**：logic 层 tierOf 改为转发 battle.TierName（原两处独立实现会漂移）；trophiesDelta 同样统一。顺带删掉 scheduleBot 里未用的 `_ = q` 和 content.go 重复段位。
- 验证：go build ./... = 0、go vet ./... = 0、go test 全过。拆分后最大文件 343 行（practice.go），battle 逻辑行为完全不变（纯移动+重命名导出）。
- 遗留超长：practice.go 343、pet.go 337、battle_api.go 256（轻度，可下次按需拆）。

## 第二轮拆分（v0.3.7，提交 66e8813，已推送确认）
- practice.go 343 → practice.go（场次生命周期 167 行）+ session_answer.go（作答/连击/XP/错题登记，188 行）。
- pet.go 337 → pet.go（接口层 168 行）+ pet_model.go（种类/食物表、等级公式、心情、petOf/petTick/petToRes，173 行）。
- 抽公共 `petLevelUp(curExp, curLevel, delta)`：PetFeed 与 addPetExp 两处相同的升级循环收敛为一份。
- 最终行数：全部文件 ≤256 行，仅 battle_api.go 256 / content.go 221 / settle.go 220 / round.go 219 四个轻微超 200（均为单 cohesive 单元，不强拆）。build/vet/test 全过。

## 第三轮：死代码清理（v0.3.8，提交 cf08d77，已推送确认）
- 核实 battle_api.go 中 applyBattleResult/battleTrophies/battleRewardTitle/battleMaxRewards 全项目零调用（battle 引擎有独立 persistResult 实现），连同本地重复的 trophiesDelta 一并删除；BattleHistory 中对手名判断的两个重复分支合并。256→152 行。
- 顺带简化 BattleHistory 对手名称逻辑（原 if/else if 两分支条件覆盖同一情形）。
- 最终：全项目仅 content.go 221 / settle.go 220 / round.go 219 三个文件 200~221 行（内聚单元不强拆），其余全部 ≤200。build/vet/test 全过。
- 推送经历 12 连败（网络中断窗口），换 20.27.177.113 第 4 次成功。教训：推送失败重试要换 IP 轮询而非固定一个。

## 用户新增备用通道：HTTP 代理 100.66.1.7:10808（当前不可用）
- 用户 2026-08-30 提供代理 `http://100.66.1.7:10808` 作为推送备用通道。实测：TCP 能连上（11ms）但 CONNECT github.com:443 隧道被 abort（curl exit 56 / git "Proxy CONNECT aborted"），socks5h 模式也不通（exit 97）——该代理当前对 GitHub 不可用，可能是节点未放行或需要认证。
- 后续若直连 + IP 轮询全失败，可重试：`git -c http.proxy=http://100.66.1.7:10808 push origin main`，代理恢复时即为可用备用通道。
- v0.3.8 最终以直连 curloptResolve 推送成功，远程 = cf08d77。origin/main 引用再次卡旧值（packed-refs），已手动校正——本机 packed-refs 不随 ls-remote 自动更新已成惯性问题，推送后一律核对。

## 技术坑：origin/main 引用不刷新（packed-refs 卡旧值）
- 现象：PowerShell fetch exit 0、ls-remote 能看到远程已是 4cd4878，但本地 `refs/remotes/origin/main` 一直卡在旧值 3f92606（fetch 不更新引用，疑似本机网络半连接导致 fetch 拿到空 refspec 也算成功）。
- 处理：直接改 `.git/packed-refs` 中 origin/main 行为目标 SHA（引用无 loose 文件、全在 packed-refs 里）。PowerShell 的 git 写 packed-refs 也无效时用文件编辑兜底。
- 推送判断以 `ls-remote` 真实输出为准，本地 origin/main 引用不可信时手动校正。

## 前端 App.vue 拆分（v0.3.9，commit c4b655d，已推送）
- 用户确认后执行：App.vue 301 行 → 壳 51 行 + 17 个新文件（19 files changed, +773/-286）。
- 结构：state.js（全局 ref 集中，避免 composable 循环依赖——关键设计：api() 需要 currentStudent/view，放 state 才不互相 import）、constants.js、utils.js、composables/（useApi/useAuth/useData/useLesson/useBattle/usePet/useAdmin）、views/（Home/Lesson/Battle/BattleHome/Pet/Admin/GlobalModals）。
- 顺带消重：选择题/填空题判分共用 applyAnswer；题目构造抽 toQuestion；学科标题三元链收敛为 subjectTitle map。
- 构建坑：GlobalModals 里 clearError/clearNotice 是函数不是 state，从 useApi.js 导入。
- 验证：npm run build 通过（111.73 kB / gzip 41.30 kB）；写了个脚本粗查模板调用函数是否漏 import。
- 行数全部达标 ≤200（最大 useLesson.js 122 行）。
- 推送：IP 探测第一个活的是 20.27.177.113，一次成功，ls-remote 核实 = c4b655d。
- LessonView 的闯关完成弹窗从全局移入组件内（result 状态归练习域）。

## v0.4.0 修复拆分遗漏的 import（commit 6ea71d8，已推送）
- 构建通过 ≠ 运行时正确：rollup 对「script 内调用了未导入的函数」不报错（tree-shake 假定其为全局），首版拆分漏了 3 处 import：
  - useAuth.js 缺 loadAdmin（进家长中心必现 ReferenceError）
  - useLesson.js 缺 enterAdmin（无学生点学科必现报错）和 computed
- 修复手段：写了个静态扫描脚本（正则找「调用了但既非 import 也非本地声明」的函数调用），复查 composables 全部通过。
- 教训：**前端拆分/搬代码后，除了 build 还要跑一次未定义标识符扫描**；vite 构建抓不住这类错误。
- 推送又遇网络抖动（CONNECT 502 / Connection reset / send-pack 断连），重试多次后经 20.27.177.113 成功，ls-remote 核实 = 6ea71d8。

## v0.4.1 修复线上 battle_scores 表缺失（commit bff0852，已推送）
- 用户报「查询段位榜失败：Table 'battle_scores' doesn't exist」
- 根因：迁移文件 000006_battle.up.sql 在代码中，但线上服务端二进制是旧版本，启动时 db.Migrate() 没跑过 000006
- 迁移文件本身正确（IF NOT EXISTS 幂等），无需改 DDL
- 修复：加 db.Migrate() 结束日志（已应用迁移数 + 最新版本），方便排查这类问题
- 实际修 bug 的操作：**用最新代码重编服务端 + 重启进程**，启动时自动迁移会建表
- 推送：20.205.243.166 一次成功，ls-remote 核实 = bff0852

## 排查「有没有人在玩」（仅调查，无代码改动）
- 结论：无真实玩家。本机 8080/8090 被另外两款 node 游戏（安邦三号/一二号）占用，studyplanet Go 后端本地未启动。
- 217 测试服务器（192.168.50.217:18180）从开发机 ping 全丢包、端口不通，无法远程查数据。
- 本地 smoke_test.db（SQLite）仅昨日冒烟测试痕迹：1 个测试账号、7 场人机局全败且 0 分、0 练习场次。
- 端口坑：以后在本机调试 studyplanet 不要默认 8080 被它占用——先确认进程身份（tasklist 查 PID）。
- 遗留观察点：昨日 7 场对战 p1 全部 0 分（挂机测试可能性大，但若用户当时答过题则判分链路有 bug，待验证）。

## v0.4.2 本地起服跑通全链路（用户说"打开"游戏）
- 环境缺口：本机无 MySQL（项目已移除 SQLite）、无 docker、8080 被别的 node 游戏（安邦三号）占用、217 内网不通（本机 192.168.28.x，217 在 192.168.50.x）。
- 方案：下载便携版 MySQL 8.0.29 到 C:/Users/liusihua/.workbuddy/binaries/mysql/（华为云镜像 214MB，40s），端口 33061（33060 被 X Plugin 占），root 空密码 + 业务账号 sp/sp123456，库 study_planet。
- **修了一个真 bug（CRLF 迁移坑）**：迁移 SQL 文件是 CRLF 行尾，db.go splitStatements 按 "\n" 切分导致 \r 残留，MySQL 报 1064 语法错。修复：splitStatements 先 ReplaceAll("\r\n","\n") 归一化。这个 bug 会在任何 Windows checkout 的环境复现。
- 启动方式：cd server 后启动 exe（resolveStaticRoot 按 cwd 找 ../client/dist 构建产物；从仓库根启动会命中 client/ 的 dev 版 index.html 导致白屏）。
- 登录通路：PIN/Casdoor 本地都不可用 → 用 node 自签 HS256 JWT（parent_id=1，secret=local-dev-secret-0123456789abcdef），手工 INSERT parents(casdoor_sub='local-dev') + children(parent_id=1)，并在 client/dist/local-login.html 写入 token 引导页（浏览器先访问一次即完成登录）。
- 种子数据：579 题（英/语/数）、10 单词、2 阅读、3 任务、学生"小朋友"(id=1, 5年级)、宠物"小狗狗"。
- 验证通过：health/students/subjects/content/pick/battle/rank/pet/weekly 全 200，WS 400 待升级正常。
- 服务启动命令（记忆备用）：cd D:/git/study_planet/server && SERVER_PORT=8095 DB_DSN='sp:sp123456@tcp(127.0.0.1:33061)/study_planet?charset=utf8mb4&parseTime=true&loc=Local' JWT_SECRET=local-dev-secret-0123456789abcdef ./studyplanet_new.exe

## v0.4.6（14:47–14:55）学生自建任务与自打钩
- 用户要求：「我的任务需要自己写，写完了自己打钩」
- **后端**：新增 `StudentAddTask` 和 `StudentDeleteTask` 公开接口（无需家长鉴权），学生可直接 POST /api/tasks 创建任务、DELETE /api/tasks/:id 删除任务
  - 学生自建任务默认5积分，上限50
  - API 定义 → Controller → Logic（tasks.go）→ Service 接口全链路
- **前端**：GlobalModals.vue "我的任务"弹窗新增输入框（写任务名 + 选类型 + 设置积分）+ 删除按钮；useAdmin.js 新增 `doStudentAddTask` / `doStudentDeleteTask`
- **安全性**：学生只能操作自己名下的任务（resolveChild + child_id 过滤）
- 构建：服务端编译通过，前端构建通过（113.17 kB JS）
- VERSION 0.4.5→0.4.6，提交 dc49be5，推送成功（140.82.112.4 活IP），远程核实 = dc49be5。
- 12 files changed, +145/-6（含2个新controller文件）

## v0.4.8（16:13–16:55）星星兑换零食放到奖励商店

- 用户要求：学习探索地图不再随机掉落零食，改为在「奖励商店」里用星星兑换——5星换小鱼干/牛奶/星星糖（随机三选一）、8星换蛋糕、15星换小火锅。
- **数据库迁移**：`000009_star_milestone.up.sql`，pets 表新增 `stars_spent INT DEFAULT 0` 记录已花费的星星数。
- **后端**：
  - 新增 `POST /api/redeem-snack` 公开接口，`stars` 参数校验 `in:5,8,15`
  - `starTotal()` 计算可用星星：`SUM(practice_sessions.stars WHERE finished=1) - pets.stars_spent`
  - `RedeemSnack()`：检查可用星星→确定零食（5星三选一随机）→原子更新 `UPDATE pets SET col=col+1, stars_spent=stars_spent+?`
  - 宠物页 SnackMsg 文案去掉"学习闯关"（改为"完成任务、对战胜利都有可能获得零食哦！"）
- **前端**：GlobalModals.vue 奖励商店弹窗新增「⭐ 零食兑换」区域，显示三档兑换按钮（5星/8星/15星），星星不足自动禁用；state.js 新增 `starTotal` 和 `busyRedeemSnack` 状态
- **验证结果**：
  - ✅ 5星兑换 → milk（随机三选一命中），剩余0星
  - ✅ 8星兑换 → 小蛋糕
  - ✅ 15星兑换 → 小火锅
  - ✅ 星星不足时返回错误码53（"星星不足，当前可用 X 颗，需要 Y 颗"）
- **VERSION 0.4.7→0.4.8**，提交 e30f7f7，14 files, +134/-7，推送成功（20.205.243.166 重试1次），远程核实 = e30f7f7。
- 服务端已重启（8095，MySQL 空密码）。
- 用户要求6项：零食显示数量+获得提示、真人对战答错不加分、经验升级显示、几胜几败、电视30分钟1000积分、同步代码
- **零食获得提示**：CompleteTaskRes / FinishSessionRes / srvMsg finished 均加 snack/snack_name 字段，前端任务完成弹窗、学习闯关完成弹窗、对战结算页面均显示"🎁 获得零食：🍎 苹果 ×1"
- **经验升级显示**：Pet API 新增 exp_to_next（还需多少经验升级），PetView 经验条下方显示"还需 X 经验升级到 Lv.Y"
- **几胜几败**：BattleRankEntry 新增 Losses 字段，BattleHomeView 显示"X 胜 Y 败"替代"X 胜 Y 战"，排行榜每位也显示"X胜Y败"
- **答错不加分**：验证 round.go 答错 p.streak=0 不加分逻辑正确，无需改
- **电视30分钟1000积分**：数据库 rewards 已 1000，seed.go 也是 1000，无需改
- VERSION 0.4.6→0.4.7，提交 537efa0（19 files, +53/-24），推送成功（20.205.243.166），远程核实 = 537efa0
- 服务端已重启（8095），MySQL 33061 运行中

## v0.4.10（17:07–17:15）任务点击打钩+上限20分
- 用户要求：任务点一下就能打钩（不用按钮），每个任务最高20积分
- **后端**：`tasks.go` 自建任务上限 50→20
- **前端**：`GlobalModals.vue` 任务行直接 `@click` 触发 `doCompleteTask`，去掉"完成"按钮保留删除按钮（`@click.stop` 防冒泡），输入框 max 50→20
- **样式**：`style.css` 新增 `.list-row.clickable` 手型光标 + hover 高亮
- **.gitignore** 完善：忽略 node_modules/dist/start.bat/start.sh/studyplanet_new.exe
- VERSION 0.4.9→0.4.10，提交 8e9cff7，推送成功（140.82.112.3），远程核实 = 8e9cff7
- 服务端已重启

## v0.4.5（14:35–21:18）宠物零食有限库存系统（掉落/消耗/惩罚）
- 用户要求：宠物投喂零食改为有限数量（初始全0），完成任务/对战胜利/学习地图获得零食，每把最多1个最少0个，概率掉落（苹果40%、小鱼干/牛奶/星星糖30%、蛋糕20%、火锅10%），饱食度到0清空积分，好感度到0清空星星，加温馨提醒。
- **实现方案**：因概率和160%异常，采用"权重法"——不掉落权重40 + 各食物权重40/30/30/30/20/10，总权重200，`grand.N(1,200)` 按区间命中保留用户给定相对稀有度。
- **数据库迁移**：`000008_pet_food_inventory.up.sql`，pets 表新增6列 `food_apple/food_fish/food_milk/food_star/food_cake/food_hotpot`，DEFAULT 0。
- **宠物模型**（`pet_model.go`）：新增 `snackDropTable` 权重表、`rollSnack()` 加权随机、`inventoryColumn(food)` 列名映射；`petTick` 扩展好感度衰减（每8小时-1），首次降到0→`clearStars`（sessions.stars/max_combo归零），饱食度降到0→`clearPoints`（删points_log）；`petToRes` 填充 FoodInv。
- **业务逻辑**（`pet.go`）：`PetFeed` 检查库存→扣减；`addSnackDrop` 调用 rollSnack 命中则 `UPDATE pets SET col=col+1`；`ExternalSnackDrop()` 闭包供 battle 引擎注入。
- **三个掉落触发点**：`tasks.go`（CompleteTask 尾）、`practice.go`（FinishSession 尾）、`settle.go`（对战胜利时 `OnSnack(p.childID)`）。
- **前端**（PetView.vue）：库存数量徽标、温馨提醒消息、文案改为"完成任务、对战胜利、学习闯关都有可能获得零食哦！"。
- **验证结果**：
  - ✅ 任务完成→掉落：星星糖 0→1，小鱼干 0→1（两次任务命中两次）
  - ✅ 学习关卡完成→掉落：星星糖 1→2（两次 session finish 中一次命中）
  - ✅ 对战胜利→掉落：代码确认（settle.go:219-221），未实际 WS 测试
  - ✅ 空库存投喂被拒（错误码53）
  - ✅ 温馨提醒返回正确
- **VERSION 0.4.4→0.4.5**，提交 1e332d1，推送成功（140.82.112.4 活IP），远程核实 = 1e332d1。
- 12 files changed, +204/-28（含迁移文件 000008）。

## v0.4.4（13:26–14:05）对战BUG修复+积分调整+本地起服
- 补推 v0.4.3（798ab84）：140.82.112.4 活IP推送成功。
- 积分调整：种子奖励"看电视30分钟"（原名看动画片）50→1000；本地库 rewards id=1 已同步 UPDATE（名称+积分+补 parent_id=1 才能在 /api/rewards 显示——种子 NULL 归属设计在本地单人环境下不可见）。
- 修了3个服务端BUG（459dd2f）：
  1. **srvMsg omitempty 吞零值**（核心）：qindex:0/score:0/correct:false 从 JSON 消失→首题渲染错位白丢一题、结算页0分空白。数值/布尔字段一律去掉 omitempty。
  2. **推题竞态**：bothDone 与超时并发双触发 nextQuestion 跳题。改为 nextQuestionFrom(fromIdx) 题号守卫，天然去重。
  3. **BattleHistory**：WhereOr 括号优先级（p1 行不受 status 过滤）+ 两真人各落一行导致重复→改显式括号分组 + room_id 去重。
- 客户端：BattleView 选项 picked class 绑定错位修复（battlePicked 是题号不是选项下标）。
- 自测：node ws 客户端全链路 5题对战，Q0 qindex 字段透传确认、27:21 胜 20奖杯青铜、落库/历史/段位接口全对。构建：server studyplanet_new.exe + client dist。
- 服务状态：MySQL 33061 + 服务端 8095 后台运行中（会话内）。用户双击 start-local.bat 可长期使用。
- 网络坑更新：GitHub IP 轮询全部失活时，DNS 直连 git push 反而能通——先试 curloptResolve 失败后可裸 push 兜底。

## 晚间（19:53–20:00）同步状态核实 + 记忆日志补推
- "继续"指令：核实仓库同步状态。发现 ahead 12 误报——fetch 已拉到 8e9cff7 但本地 packed-refs 未更新（惯性坑再现），直接编辑 packed-refs 校正后确认本地与远程完全同步，12 个提交实际均已在线。
- 工作区剩余改动（v0.4.10 日志补记）单独提交 13a05fc，push 成功，ls-remote 核实远程 = 13a05fc；packed-refs 再次手动校正到 13a05fc。
- 网络备注：本次 /dev/tcp IP 轮询全部超时（exit 124），但 curloptResolve=20.205.243.166 的 push/ls-remote 直接成功——该 IP 仍是首选。
