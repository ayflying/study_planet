# 学霸星球登录会话持久化修复

- 家长登录 token 继续使用浏览器 `localStorage` 保存，刷新页面、关闭并重新打开浏览器后仍会自动恢复登录状态。
- 服务端 JWT 移除 30 天固定过期时间；只要 JWT 密钥不变，登录状态不会因时间到期掉线。
- 仅在家长主动点击“退出登录”、服务端更换 `JWT_SECRET`，或浏览器主动清理站点数据时结束会话。
- 接口返回 401 时前端会清理失效 token 并回到登录引导，避免页面显示假登录状态。
- 服务端 `go test ./...` 通过；客户端构建需先安装/恢复 `client/node_modules` 后执行。